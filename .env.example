# Environment configuration for HSO Data Extractor
# Copy this file to `.env` and fill values for local development.

# App
ENV=development
DEBUG=true
LOG_LEVEL=DEBUG
APP_PORT=8501

# Azure (set in production via App Settings, not .env)
USE_MOCK_AZURE=true
AZURE_FUNCTION_URL=
AZURE_STORAGE_ACCOUNT=

# Secrets (use Azure Key Vault in production)
# EXAMPLE_API_KEY=
